{
  "collectionName": "components_deep_callouts",
  "info": {
    "displayName": "Callout",
    "icon": "exclamation",
    "description": "Highlighted callout block (note/warning/insight) for design trade-offs and key takeaways."
  },
  "options": {},
  "attributes": {
    "variant": {
      "type": "enumeration",
      "enum": ["note", "insight", "warning", "quote"],
      "default": "insight",
      "required": true
    },
    "title": {
      "type": "string"
    },
    "body": {
      "type": "text",
      "required": true
    }
  }
}
